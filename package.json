{
  "name": "openapi-website",
  "version": "0.0.1",
  "license": "MIT",
  "description": "LongPort OpenAPI docs",
  "private": false,
  "scripts": {
    "docusaurus": "docusaurus",
    "dev": "npm run dev:docs",
    "dev:en": "npm run dev:docs --locale en",
    "dev:zh-HK": "npm run dev:docs --locale zh-HK",
    "dev:docs": "docusaurus start --port=8000",
    "dev:swagger": "cross-env WATCH=1 node swagger-watch.js",
    "build:swagger": "node swagger-watch.js",
    "build": "npm run clear && yarn build:swagger && npm run build:canary",
    "build:release": "npm run clear && yarn build:swagger && docusaurus build --out-dir dist",
    "build:canary": "npm run clear && yarn build:swagger && docusaurus build --out-dir dist",
    "clear": "docusaurus clear && mkdir -p dist && rm -rf dist",
    "serve": "docusaurus serve --dir dist --port=8008",
    "create:docs": "docusaurus docs:version",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "swizzle": "docusaurus swizzle",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@docusaurus/core": "^3.2.1",
    "@docusaurus/preset-classic": "^3.2.1",
    "@docusaurus/theme-mermaid": "^3.2.1",
    "@mdx-js/react": "^3",
    "@saucelabs/theme-github-codeblock": "^0.1.1",
    "ahooks": "^3.3.10",
    "autoprefixer": "^10.4.19",
    "classnames": "^2.3.1",
    "js-cookie": "^3.0.1",
    "postcss-mixins": "^10.0.1",
    "prism-react-renderer": "^2",
    "react": "^18.2",
    "react-dom": "^18.2",
    "react-photo-view": "^1.0.0",
    "tailwindcss": "^3.4.3"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "^3.2.1",
    "@docusaurus/types": "3.0.0",
    "@tsconfig/docusaurus": "^1.0.5",
    "concurrently": "^7.0.0",
    "cross-env": "^7.0.3",
    "docusaurus-plugin-sass": "^0.2.5",
    "js-yaml": "^4.1.0",
    "json-beautify": "^1.1.1",
    "node-watch": "^0.7.3",
    "postcss": "^8.4.38",
    "sass": "^1.75.0",
    "sass-loader": "^14.2.1",
    "typescript": "^5",
    "widdershins": "^4.0.1"
  },
  "browserslist": {
    "production": [">0.5%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  }
}
