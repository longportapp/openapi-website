swagger: "2.0"
info:
  description: "更新關註分組"
  version: "v1"
  title: "更新關註分組"
paths:
  /v1/watchlist/groups:
    put:
      access:
      parameters:
        - name: "id"
          type: "integer"
          description: "分組 ID"
          required: true
        - name: "name"
          type: "string"
          description: "分組名稱"
          required: true

      responses:
        "200":
          description: "返回成功"
        "500":
          description: "內部錯誤"
      request_body:
        type: "python"
        value: |

      response_body: { "code": 0 }
