swagger: "2.0"
info:
  description: "創建關註分組"
  version: "v1"
  title: "創建關註分組"
paths:
  /v1/watchlist/groups:
    post:
      access:
      parameters:
        - name: "name"
          type: "string"
          in: body
          description: "分組名稱"
          required: true
        - name: "securities"
          type: "string[]"
          in: body
          description: "分組下的股票列表。列表中的順序將作為展示順序"
          required: false

      responses:
        "200":
          description: "返回成功"
          schema:
            $ref: "#/definitions/create_group_response"
        "500":
          description: "內部錯誤"
      request_body:
        type: "python"
        value: |

      response_body: { "code": 0, "data": { "id": 10086 } }

definitions:
  create_group_response:
    properties:
      id:
        type: "integer"
        description: 分組 ID
        required: true
