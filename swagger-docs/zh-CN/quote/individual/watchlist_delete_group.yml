swagger: "2.0"
info:
  description: "删除关注分组"
  version: "v1"
  title: "删除关注分组"
paths:
  /v1/watchlist/groups:
    delete:
      access:
      parameters:
        - name: "id"
          type: "integer"
          description: "分组 ID"
          required: true
        - name: "purge"
          type: "boolean"
          description: "是否清除分组下的股票"
          required: true

      responses:
        "200":
          description: "返回成功"
        "500":
          description: "内部错误"
      request_body:
        type: "python"
        value: |

      response_body: { "code": 0 }
