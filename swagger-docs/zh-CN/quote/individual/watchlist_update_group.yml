swagger: "2.0"
info:
  description: "更新关注分组"
  version: "v1"
  title: "更新关注分组"
paths:
  /v1/watchlist/groups:
    put:
      access:
      parameters:
        - name: "id"
          type: "integer"
          description: "分组 ID"
          required: true
        - name: "name"
          type: "string"
          description: "分组名称"
          required: true

      responses:
        "200":
          description: "返回成功"
        "500":
          description: "内部错误"
      request_body:
        type: "python"
        value: |

      response_body: { "code": 0 }
